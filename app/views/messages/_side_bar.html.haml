%div{class: "side_bar"}
  %div{class: "side_bar__groups"}
    -# - if @groups.present?
    -#   - @groups.each do |group|
    -#     = render partial: "group", locals: {group: group}
    = render "group"
